/*
  `Note: childNodes is looks like an array but it is not an array it is an
node's list To use all array methods on it we have to first convert to 
it in an array by using Array.from() method then we can use it as an array.`
*/

/*
console.log( document, "<=== Document " )
console.log( document.firstChild, "<=== Document firstChild " )
console.log( document.childNodes, " Document child nodes" )
console.log( document.childNodes[ 0 ], " Document child nodes[0] i.e. first child" )
console.log( ( Array.from( document.childNodes ) ), "childNodes converted to an array" )
*/