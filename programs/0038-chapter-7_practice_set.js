// Q1) Create a navbar and change the color of first element to red.

// Ans===>

// let firstElemnt = document.getElementById( "home" ).style.color = "red"

// Q2) Create a table without tbody and open the page source to check the tbody is present or not...?

// Ans===>

// It comes automatically by default in inspect not in view page source

/*
Q3) Create an element with three children now change the
color of first and last element to green
*/

// Ans===>

// let change = document.querySelectorAll( ".paraChngeColor" )
// console.log( change, "change" );
// change[ 0 ].firstElementChild.style.color = "green"
// change[ 0 ].lastElementChild.style.color = "green"

// Q4) Write a JS code to change all background of <p> tags to cyan

// Ans===>

// let tag = Array.from( document.getElementsByTagName( "p" ) ).forEach( ( element ) =>
// {
//   element.style.background = "cyan"
// } )

// console.log( tag, "tag" );

// tag[ 0 ].style.background = "cyan"
// tag[ 1 ].style.background = "cyan"
// tag[ 2 ].style.background = "cyan"

// Q5) Which of the following is used to look for the farthest ancestor that matches a given css selector

// ( a ) matches
// ( b ) closest
// ( c ) contains
// ( d ) none of these

// Ans===>

// ( d ) none of these



