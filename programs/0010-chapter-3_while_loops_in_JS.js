const prompt = require( "prompt-sync" )()
/* 
// While loops in JS


N = prompt( "Enter A Number ===> : " )

N = Number.parseInt( N )

while ( N < 20 )
{
  console.log( N )
  N++               // necessary otherwise JS runtime may crash due to infinite looping
}


// do-while loop

N = prompt( "Enter A Number ===> : " )
N = Number.parseInt( N )
do
{

  console.log( N + 3 );
  N++     // important otherwise if the condition gets true then it may crash JS runtime 
} while ( N < 0 )


*/